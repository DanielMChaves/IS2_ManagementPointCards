<h1>Editing purchase</h1>

<%= form_for :purchase, url: purchase_path(@purchase), method: :patch do |f| %>
  <% if @purchase.errors.any? %>
    <div id="error_explanation">
      <h2>
        <%= pluralize(@purchase.errors.count, "error") %> prohibited
        this purchase from being saved:
      </h2>
      <ul>
        <% @purchase.errors.full_messages.each do |msg| %>
          <li><%= msg %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <p>
    <%= f.label :id_store%><br>
    <%= f.text_field :id_store %>
  </p>

  <p>
    <%= f.label :buy_date %><br>
    <%= f.text_field :buy_date %>
  </p>

  <p>
    <%= f.label :total %><br>
    <%= f.text_field :total %>
  </p>

  <p>
    <%= f.submit %>
  </p>
<% end %>

<%= link_to 'Back', purchases_path %>
