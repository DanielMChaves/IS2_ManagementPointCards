<h1>Esto es el formulario de crear premio</h1>

<%= form_for :price, url: prices_path do |f| %>
  <% if @price.errors.any? %>
    <div id="error_explanation">
      <h2>
        <%= pluralize(@price.errors.count, "error") %> prohibited
        this price from being saved:
      </h2>
      <ul>
        <% @price.errors.full_messages.each do |msg| %>
          <li><%= msg %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <p>
    <%= f.label :title %><br>
    <%= f.text_field :title %>
  </p>

  <p>
    <%= f.label :text %><br>
    <%= f.text_area :text %>
  </p>

  <p>
    <%= f.submit %>
  </p>
<% end %>

<%= link_to 'Volver a mis premios', prices_path %>
<p><%= link_to 'Mis premios', controller: 'prices' %></p>
